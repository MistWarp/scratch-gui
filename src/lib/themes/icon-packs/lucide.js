import * as Lucide from 'lucide-react';

const name = 'Lucide';

const guiColors = {};

const blockColors = {};

export const icons = Lucide;
export { name as displayName, name, guiColors, blockColors };
