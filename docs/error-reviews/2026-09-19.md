# Error report review, 19 September 2026

This review covers the supplied export, whose `openCount` is 83. Fixes are local and have not been deployed. No production reports have been marked resolved.

## Local fixes

| Report | Cause and change |
| --- | --- |
| `eb1ENU2O8bMrzvDho`, line tool `lastSegment` | Short-line mouse-up paths cleared the path without ending the gesture. End it before all early returns and image callbacks, ignore drags without a path, and end gestures on deactivation. |
| `eWbMdBPuTjFvHcXgg`, `oldBlock is not rendered` | Duplicate runs after a timeout and can receive a block disposed while the menu was open. Cancel the action when its workspace or SVG is gone. Rebuilt both Blockly bundles. |
| `eHVvfrey5EMWG2U3d`, missing `getAllVariableNamesInScopeByType` | Sensing menus assumed the stage existed while targets were changing. Keep the built-in options when it is absent. Apply the same missing-target handling to Spotlight menus. |
| `eAQTeZP5kmZvf6FeN`, missing `onOpenExtensionLibrary` | The menu depended on a callback supplied by its parent. Give the connected menu its own Redux action binding. |

## Already present in the current code

- `CalendarDays` is imported in `community/pages/Project.jsx`.
- `rankSections` is imported in `community/components/NavBar.jsx`. The supplied undefined-name reports are from development builds.
- Community lazy imports retry once and reload once per build when a chunk request fails. All supplied dynamic-import failures identify crawlers in their user agents. This does not establish whether the cause was a missing deployed asset, stale cached references, or another transport failure. Existing recovery is not proof that those reports are resolved.
- The editor camera provider already handles rejected playback promises. The supplied fullscreen media failures have no stack identifying their caller, so that handling cannot be assumed to cover them.

## Still unresolved

| Failure group | Evidence and next diagnostic needed |
| --- | --- |
| IndexedDB security-context denial | LightningFS initialization is in the stack. Project loading calls `deleteRepo`, which requires this storage and can propagate the error into the GUI. Reproduce the iPad storage restriction and provide a deliberate unavailable-storage behavior that preserves project/history recovery guarantees. The existing Web Locks patch does not solve an IndexedDB denial. |
| Renderer shader linking | The renderer reports a program-link failure for the color effect. The driver supplied no link-log detail. Reproduce with the reported browser/GPU and inspect WebGL context loss and shader logs before changing shader behavior. |
| `Shaders.js` compilation | The external extension rejects shader source beginning with `vertex` or `0`. Its source is not in this workspace. The triggering blocks and shader text are needed to distinguish invalid input from an extension defect. |
| `mem is not defined`, invalid array length | Errors originate in generated project code. No `mem` reference was found in the local VM compiler source. Need the project and generated function, including extension compiler hooks. |
| ThreeJS `fromArray`, OriginFS `rec`, `VirtualFileSystem` redeclaration, hyperSense `unsandboxed` | External or embedded extension code appears in the reports. The huge data-URL stacks are truncated before useful caller locations. Need the exact extension versions and triggering projects. |
| Relative extension paths during Fractch live reload | `extensions/OriginFS.js`, `extensions/OSLUtils.js`, and `extensions/HyperSenseSP.js` were requested while the project came from localhost. The current VM resolves relative URLs against the page URL. Verify the producer's intended extension base and security policy before changing resolution. |
| SoundExpanded script load and Mist's Utils fetch | External network requests failed. Need the failed response, CORS/network details, and extension version. |
| Generic network errors | No operation or caller appears in the stacks. Several share embedded project `p1788328877399073000bNhXl0`; reproduce that project's load with network logging. |
| Pointer-lock denial, media interruption/autoplay/unsupported source, rejected tab permission | Browser or permission outcomes reached the global rejection handler. Handle them at their originating calls once identified; do not globally suppress unrelated errors by message. |
| `Script error.` | Cross-origin reports conceal the exception and usually have no location. Some name external extension scripts. Need an unredacted console stack or a reproduction. |

## Verification

- Regression tests cover short-line cancellation and extension, late pointer events, starting a new line after cancellation, disposed/live duplication, stage removal and return, and extension-library dispatch without a parent callback.
- Existing menu workflow and lazy-import recovery tests also pass.
- Targeted ESLint and whitespace checks pass. Paint tests require the Jest and browser environments on the ESLint command line.
- Blockly's local Closure build completed with zero errors and one warning.
- The full Vite site production build completed successfully. It reports bundle-size warnings.
